package fr.alexandreklotz.quickdesk.controller;

public class PermissionController {

    /*Will probably be deleted, there will be pre-defined permissions such as read permissions, writing permissions, etc... The model will be @Embeddable
    i cannot code in a way that makes custom permissions to work. */
}
